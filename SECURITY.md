# 보안

## 취약점 신고

**공개 이슈로 올리지 마세요.** GitHub 의
[Security Advisory](../../security/advisories/new) 로 비공개 신고해 주세요.

## 대응 — **목표이지 SLA 가 아니다**

| | 목표 |
|---|---|
| 접수 확인 | 영업일 기준 3일 이내 |
| 심각도 medium 이상 수정 | 60일 이내 |

🔴 **이것은 최선 노력 목표다.** 1인 프로젝트라 교대 대응 체계가 없다 —
**지킬 운영 능력이 없는 약속을 문서에 적지 않는다.** 급한 건이면 신고에 그렇게 적어 주세요.

## 이 저장소가 하는 것

- 의존성은 **락파일로 고정**되고 Dependabot 이 주 1회 갱신을 연다
- GitHub Actions 는 **커밋 SHA 로 핀**한다 — 태그는 가변이라 공급망 벡터다
- 시크릿은 커밋하지 않는다. `.env` 는 ignore 되고 `.env.example` 만 커밋된다
- `main` 은 보호돼 있고 CI 초록 없이는 머지되지 않는다

## 하지 않는 것

**자체 제작 암호를 쓰지 않는다.** 표준 라이브러리와 검증된 구현만 쓴다.

# 기여

## 흐름

**브랜치 → PR → CI 초록 → 머지.** `main` 직접 푸시는 거부된다(소유자 포함).

크기가 있는 변경은 **이슈를 먼저** 연다 — 인수기준을 `AC-n` 으로 박기 위해서다.
오타 같은 사소한 변경은 바로 PR 로 온다. 모든 PR 에 이슈를 요구하지 않는다.

## PR 크기

**200줄을 목표로 하고, 400줄을 넘기지 않는다.** 크면 쪼갠다.

근거는 SmartBear/Cisco 케이스 스터디의 결론이다 —
*"LOC under review should be **under 200, not to exceed 400**."*
**400은 목표가 아니라 상한**이고, 리뷰 효과가 떨어지기 시작하는 지점은 **200 부근**이다.

⚠️ 이건 **한 조직(Cisco 1개 팀·50명·2005~2006)의 케이스 스터디**다. *"모든 연구가 일치한다"* 가
아니라 *"가장 큰 사례 연구 하나가 이렇게 말한다"* 이다. 저자 스스로 밝힌 암묵 가정도 있다
(변경 크기와 무관하게 결함 밀도가 일정하다고 가정).

**세는 건 사람이 읽을 diff 다.** 락파일·생성물·대량 이동은 따로 센다 — 생성물이 1,900줄이면
사람이 읽을 120줄이 그 안에 묻힌다.

## 테스트 정책

- **동작이나 버그가 바뀌면 그 변화를 잡는 테스트를 같은 PR 에 넣는다. 해당하지 않으면 이유를 적는다.**
  ⚠️ *"소스가 바뀌면 테스트도"* 가 아니다 — 문서·설정·리팩터링처럼 **동작이 그대로인 변경**에
  의미 없는 테스트 수정을 유도하지 않기 위해서다. 기준은 **파일이 바뀌었나**가 아니라 **동작이 바뀌었나**다.
- 실패하는 테스트를 **먼저** 쓰는 것을 권한다 — 강제하지는 않는다.
  (순서 자체의 효과는 증거가 갈린다. 강제할 만큼 확실하지 않다.)
- 새 동작에는 그 동작을 **실제로 실행하는** 테스트가 있어야 한다.
  커버리지 숫자가 아니라 **그 테스트가 없으면 깨진 걸 못 잡는가**가 기준이다.
- `filterwarnings = ["error"]` 다. 경고는 쌓이지 않는다 —
  서드파티 경고로 막히면 좁은 `ignore` 를 한 줄 추가하고 이유를 남긴다.

## 인수기준

이슈의 `AC-n` 은 **안정 ID** 다. 번호를 재사용하거나 갈아끼우지 않는다.
PR 은 어느 AC 를 닫는지, 무엇이 그것을 증명하는지 밝힌다.
증명할 검사를 정할 수 없으면 **`UNVERIFIABLE` 로 표시한다.** 조용히 통과시키지 않는다.

## 커밋·PR 제목

본문 형식은 강제하지 않는다. **PR 제목**만 `type(scope): 요약` 으로 쓴다 —
squash 로 머지하므로 **PR 제목이 곧 랜딩 커밋**이다. `ci / pr-title` 이 검사한다.

**타입은 이 11종뿐이다** (`@commitlint/config-conventional`):
`feat` `fix` `docs` `style` `refactor` `perf` `test` `build` `ci` `chore` `revert`

🔴 **새 타입을 만들지 마라.** 남는 의미는 **scope** 로 쓴다 — `docs(research):` ·
`docs(decision):` · `refactor(layout):` · `build(deps):` · `fix(security):`.
규격이 그렇게 시킨다(타입은 `feat`·`fix` 만 필수이고 **scope 는 자유형**이다).
이 규칙이 있는 이유: 커스텀 타입은 **적어둬도 안 읽힌다.** 실제로 그래서 한 번 갈렸다.

## 로컬에서 먼저

```bash
uv sync --locked
uv run ruff check . && uv run ruff format --check .
uv run mypy .
uv run pytest
```

CI 는 이것들을 각각 별도 검사로 돌리고 **시크릿 탐지·CodeQL 도 함께** 돈다.
여기서 통과하면 그 넷은 CI 에서도 통과한다. **검사 목록의 정본은 저장소 룰셋이다.**

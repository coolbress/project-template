# 🔴 **빈 이슈를 막지 않는다** — 선택이고, 그 이유를 여기 적는다(침묵과 기각은 다르다).
#
# 폼을 강제하면 *"모든 것에 양식"* 이 되고, 이 프로젝트는 그 형태를 이미 기각했다 —
# `standards` `direction/04` §명시적으로 원하지 않는 것: *"모든 변경에 이슈를 요구하면
# 사람이 규칙을 우회하기 시작한다."* 메모 한 줄을 남기려고 양식을 채우게 하면 안 남긴다.
#
# ⚠️ **`contact_links` 는 안 쓴다** — 가리킬 곳이 없다. Discussions 는 기각됐고
# (*"1인 프로젝트에 포럼은 빈 방이다"*) 별도 문서 사이트도 없다.
# 빈 링크를 두면 그것이 `presence≠adequacy` 다.
blank_issues_enabled: true
